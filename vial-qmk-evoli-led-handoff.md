# ydkb/evoli (Agar Mini EC) — RGB 인디케이터 LED 디버깅 인계 문서

이 문서는 `vial-qmk-evoli-migration-handoff.md`(컴파일 에러 마이그레이션 문서)의 후속편이다.
**컴파일/매트릭스/키 입력은 이미 정상 동작 확인됨** (커밋 `ddd00c81ad`: "컴파일 및 정상동작 성공"). 남은 문제는 RGB 인디케이터 LED 하나뿐이다.

## 하드웨어 개요

- 보드: ydkb/evoli, 키맵 `agar_mini_ec_vial` (Agar Mini EC, MATRIX_ROWS=6, MATRIX_COLS=8)
- PCB에 물리적으로 **LED가 단 하나** 존재. 라벨 `RGBL1`, 4핀 SMD 패키지 — 사용자가 사진으로 직접 확인. 드라이버 내장형 어드레서블(WS2812 계열) LED로 추정.
- 부트로더: "Double Tap Flash Mode" (PCB 실크스크린 문구). Maple Mini 계열 STM32duino 부트로더 컨벤션으로 추정.
- MCU: STM32F103, ChibiOS, `BOARD = MAPLEMINI_STM32_F103`

## 1. 문제 정의

펌웨어(메인 QMK)가 부팅된 뒤, RGBL1 LED가:
- 캡스락 on/off와 무관하게 반응 없음
- 부팅 테스트 시퀀스(빨강→초록→파랑→꺼짐)를 적용해도 반응 없음
- 임의의 키 입력에도 반응 없음

하지만 **부트로더 모드(메인 펌웨어 진입 전, 별도 코드)에서는 같은 LED가 색을 바꾼다** — 사용자가 직접 확인. 즉 LED 자체는 살아있고 제어 가능하지만, 우리 메인 펌웨어 코드가 보내는 신호는 지금까지 단 한 번도 확실하게 LED에 영향을 준 적이 없다 (아래 타임라인 참고).

## 2. 시도한 방법과 결과 (시간순)

| # | 시도 | 결과 |
|---|---|---|
| 1 | `WS2812_DI_PIN B15` (원래 풀사이즈 evoli config.h에서 그대로 물려받은 값)로 부팅 테스트(`CONFIG_BOOT_TEST_RGB`, 빨강→초록→파랑→꺼짐) 실행 | **실패** — 처음부터 끝까지 계속 흰색, 색 전환 전혀 안 보임 |
| 2 | B15에서 캡스락 토글 | **실패** — 무반응, 항상 흰색 |
| 3 | PCB 사진으로 RGBL1이 단일 4핀 SMD 어드레서블 LED임을 확인 (별도 비제어 전원 LED 가설 배제) | 확인됨 (하드웨어 사실) |
| 4 | B15에 raw GPIO 토글(WS2812 프로토콜 무시, 단순 0.5초 on/off) | **실패** — 무반응 |
| 5 | PC13(STM32F103 "Blue Pill" 계열 보드의 통상적 온보드 LED 핀)에 raw GPIO 토글 | **실패** — 무반응 |
| 6 | (재검토) raw GPIO 토글 자체가 부적절한 테스트일 수 있음을 인지 — WS2812류 LED는 프로토콜에 안 맞는 신호를 그냥 무시하고 마지막 상태를 유지하는 경우가 많아서, "무반응"이 "핀이 틀림"을 증명하지 못함 | 진단 방법론 결함 인지 |
| 7 | 사용자가 부트로더 모드에서 같은 RGBL1이 색을 바꾸는 것을 확인 → 웹 검색으로 STM32duino 부트로더("Double Tap")가 관례적으로 PB1을 LED 핀으로 쓴다는 정보 입수 | 정보 획득 (단서) |
| 8 | **단, B1은 `ec_select_row()`에서 매트릭스 row 1 스캔용으로 이미 쓰이고 있음** (row 번호 == GPIOB 핀 번호로 직접 매핑) — 충돌 가능성 플래그 | 모순 인지, 사용자에게 확인 요청 |
| 9 | 사용자가 "물리적으로 LED가 하나뿐"이라고 재확인 → `WS2812_DI_PIN`을 B15 → **B1**으로 변경 (핀을 매트릭스 row와 시분할 공유한다는 가정) | 변경 적용 |
| 10 | B1로 변경 후, **부트로더를 거치지 않고 바로 일반모드로 켰을 때** LED가 흰색→꺼짐으로 바뀜 | **처음엔 성공으로 보였음** (하지만 11번에서 재해석됨) |
| 11 | 부트로더→일반모드 전환(소프트 리셋, 전원 재투입 없이) 시에는 부트로더가 마지막으로 표시하던 색(빨강/초록/파랑, 타이밍에 따라 다름)이 그대로 유지됨 — 사용자의 설명 | 별도 현상으로 일단 분리해둠 |
| 12 | 캡스락 켰을 때 청록색이 뜨도록 `led.c`의 `user_config_init()`에서 `indicator_color[0]`을 VIA 레이아웃 옵션 기반 계산 대신 고정 청록 `{0,255,255}`로 덮어씀 | 코드 변경 완료, **테스트 결과: 실패** — 캡스락 무반응 |
| 13 | B1이 매트릭스 row와 공유되는 핀이라 매트릭스 스캔이 핀 모드를 계속 INPUT_PULLUP↔OUTPUT_PUSHPULL로 바꿔버려서, 부팅 시 1회성 호출(스캔 시작 전)은 성공하지만 이후의 모든 호출(부팅 후 발생하는 모든 이벤트)은 실패한다는 가설 수립 → `my_rgblight_flush()`와 `set_rgb_user()`에서 `ws2812_flush()` 호출 직전에 `gpio_set_pin_output(WS2812_DI_PIN)`을 강제 호출하도록 수정 | **테스트 결과: 실패** — 동일 증상 지속 |
| 14 | "호스트의 USB LED 리포트가 `led_update_user()`까지 도달하지 않는 것"과 "WS2812 쓰기 자체가 깨진 것"을 분리하기 위해, **호스트 LED 리포트와 무관하게 아무 키 입력 시 인디케이터를 직접 토글**하는 진단 코드(`DIAG_FORCE_INDICATOR_ON_CAPS`)를 `process_record_user()`에 추가 (안전한 동기 컨텍스트에서 직접 호출) | **테스트 결과: 실패** — 어떤 키를 눌러도 무반응. 이건 "호스트 리포트 문제"설을 무력화시키고, WS2812 쓰기 경로 자체가 부팅 이후 시점에 작동하지 않는다는 쪽으로 의심을 좁힘 |
| 15 | (재해석) 13번까지의 "B1으로 바꾼 뒤 콜드부팅 시 꺼짐" 결과(10번)도, 사실은 우리 코드가 아니라 **부트로더가 정상 종료(더블탭 플래시 모드 진입 없이)할 때 자체적으로 LED를 꺼두는 동작일 수 있다**는 가능성 제기 — 즉 우리 메인 펌웨어가 이 LED에 영향을 준 것을 "확실하게" 증명한 사례가 지금까지 **0건**일 수 있음 | 가설 (미확정) |
| 16 | (진행 중, 결과 미확인) `DIAG_B15_BLINK`를 재활성화하되 이번엔 `WS2812_DI_PIN`(=B1) 자체를 매 사이클마다 강제로 출력 모드 재설정하면서 0.5초 토글하도록 수정. 매트릭스 스캔이 한참 돌고 있는 *post-boot* 상태에서 B1을 전기적으로 흔드는 것 자체가 가능한지 확인하는, 가장 기초적인 다음 테스트 | **결과 대기 중** (사용자가 아직 보고 안 함 — 다음 세션에서 이어갈 부분) |

### 정리: 뭐가 먹혔고 뭐가 안 먹혔나

- **먹힌 것 (확실)**: 없음. (10번의 "성공"은 15번에서 우연/부트로더 동작일 가능성으로 재해석됨 — 미확정)
- **안 먹힌 것 (확실)**:
  - B15/PC13/B1 각각에 대한 raw GPIO 토글 — 전부 무반응 (단, 이 테스트 자체가 WS2812 칩 특성상 신뢰도가 낮을 수 있음, 6번 참고)
  - B15 + 정식 WS2812 프로토콜(부팅 테스트) — 무반응
  - B1 + 정식 WS2812 프로토콜(부팅 테스트, 캡스락 연동, 아무 키 입력 연동, 핀모드 강제 재설정 포함) — 전부 무반응 (단, 부팅 시점의 1회성 호출 결과는 모호함, 15번 참고)

## 3. 코드 변경 현황 (이번 세션, 파일별)

### `keyboards/ydkb/evoli/led.c`
- `led_set_user(uint8_t)` → `bool led_update_user(led_t led_state)` 로 시그니처 변경 (최신 QMK API, `led_state.raw` 사용). **이건 확실히 필요한 정상 수정이고 컴파일 이슈와 직결됨.**
- `my_rgblight_flush()`, `set_rgb_user()`: `ws2812_flush()` 호출 직전에 `gpio_set_pin_output(WS2812_DI_PIN)` 추가 (B1-매트릭스 공유 대응 시도, 효과 미확인/실패로 보임)
- `user_config_init()`: `indicator_color[0]`을 VIA 레이아웃 옵션 계산 결과 대신 고정 청록 `{0,255,255}`로 덮어씀 (사용자 요청, 캡스락 연동이 실제로 동작하기 시작하면 의도대로 작동할 것)
- `process_record_user()`: `#ifdef DIAG_FORCE_INDICATOR_ON_CAPS` 블록 — **임시 진단 코드**, 아무 키 입력 시 `indicator_state` 토글 + `rgblight_set()` 직접 호출. (작업 중 `break`만 쓰고 `return` 없는 undefined-behavior 버그를 발견해서 수정한 바 있음 — 비슷한 패턴 작성 시 주의)
- `hook_keyboard_loop()` / `diag_b15_blink()`: `#ifdef DIAG_B15_BLINK` 블록 — **임시 진단 코드**, `WS2812_DI_PIN`을 0.5초마다 raw 토글 (현재 진행 중인 테스트, 결과 대기)

### `keyboards/ydkb/evoli/config.h` (보드 공통, 두 키맵 모두 공유)
- `WS2812_DI_PIN`: `B15` → `B1` (매트릭스 row 1과 공유 가정, 효과 미확정)

### `keyboards/ydkb/evoli/keymaps/agar_mini_ec_vial/config.h`
- `USB_LED_CAPS_LOCK` (제거된 심볼) → `INDICATOR_FUNCT` 매크로에서 리터럴 `(1<<1)`로 교체 — **정상 수정, 컴파일 이슈 해결**
- `RGBLED_NUM`: `0` → `1` (rgblight 코어의 div-by-zero/array-bounds 회피) — **정상 수정**
- `WS2812_LED_COUNT`: `(PHY_INDICATOR_NUM + RGBLED_NUM)` 추가 — **정상 수정** (RGBLIGHT_DRIVER=custom일 때 자동 정의 안 되는 값)
- `CONFIG_BOOT_TEST_RGB` — 활성화 (진단용 부팅 테스트)
- `DIAG_B15_BLINK` — **현재 활성화** (16번 테스트, 결과 대기)
- `DIAG_FORCE_INDICATOR_ON_CAPS` — **현재 활성화** (14번 테스트, 실패 확인됨 — 다음 세션에서 끄거나 다른 진단으로 교체 검토)

### `keyboards/ydkb/evoli/keymaps/agar_ec_vial/config.h` (풀사이즈 키맵, LED 디버깅은 안 했지만 컴파일 수정은 동일 적용)
- `USB_LED_CAPS_LOCK` → `(1<<1)` 로 교체 (mini 키맵과 동일한 수정)

## 4. 최종적으로 남은 문제

**RGBL1 LED가 메인 QMK 펌웨어로부터 어떤 식으로도 제어되지 않는다.** 부트로더는 확실히 이 LED를 제어할 수 있으므로 하드웨어 자체는 문제가 없다고 판단됨. 가능한 원인 두 가지가 미해결 상태로 남아있고, 다음 세션에서 우선순위는 아래 순서를 추천:

1. **(최우선, 진행 중) B1을 post-boot 시점에 raw하게 토글했을 때 전기적으로 흔들리는지 확인** (`DIAG_B15_BLINK`, 16번 항목) — 이 결과가 안 나왔음. 이게 안 흔들리면 B1도 틀린 핀이거나, 매트릭스 스캔과의 공유가 생각보다 더 근본적으로 충돌하고 있다는 뜻.
2. 만약 raw 토글도 무반응이면: **멀티미터로 RGBL1의 DIN 패드 ↔ MCU 핀 사이 연속성(continuity)을 직접 측정**하는 게 가장 확실한 차선책. (이전에 한 번 제안했으나 사용자가 소프트웨어적 방법을 먼저 원해서 보류된 상태)
3. B1이 매트릭스 row와 진짜로 공유되는 핀이 맞다면, 시분할 자체가 아니라 **매트릭스 스캔 루프와 WS2812 전송이 동시에 실행될 때 서로 어떻게 상호작용하는지**(타이밍, chSysLock 중첩 여부 등)를 더 깊이 봐야 할 수도 있음.
4. `DIAG_FORCE_INDICATOR_ON_CAPS`, `DIAG_B15_BLINK`, `CONFIG_BOOT_TEST_RGB`는 전부 **임시 진단 코드**이므로, 문제 해결 후 반드시 제거(또는 `#undef`)해야 함.
5. 캡스락 색상을 VIA의 기존 "CapsLock Color" 레이아웃 옵션(빨강/주황/노랑/초록/청록/파랑/보라/흰색, `vial.json` 참고) 대신 고정 청록으로 하드코딩한 부분(`led.c` 12번 항목)은 **LED 제어 자체가 살아나면 그대로 둘지, VIA 옵션을 살릴지 사용자와 재논의 필요**.

## 5. 참고: 이전 마이그레이션 문서에서 이미 해결된 것들 (재확인됨, 이번 세션에서 재점검)

- `select_row` 미사용 함수 — 이미 주석 처리되어 있었음, 추가 조치 불필요
- `CONSOLE_ENABLE ?= yes` — 이미 적용되어 있었음
- `WS2812_DRIVER = bitbang` — 이미 설정되어 있었음 (하드웨어가 진짜 bitbang인지 i2c인지는 이번 세션에서 핀 자체가 안 맞아서 별 의미 없어짐 — RGBL1이 라벨상 단일 어드레서블 LED이므로 bitbang 가정 자체는 맞는 것으로 보임)
- `led_set_user`/`USB_LED_CAPS_LOCK` 컴파일 에러 — 이번 세션에서 해결 (`led_t`/`led_update_user` API로 전환)
